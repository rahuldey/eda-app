const styles = (theme) => {
  return {
    item: {
      marginBotton: theme.spacing(3),
      marginTop: theme.spacing(3),
      border: "1px solid black",
    },
    img: {
      width: "100%",
    },
  };
};

export default styles;
